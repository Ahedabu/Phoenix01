@model EditHobbiesViewModel

@{
    ViewData["Title"] = "Hobbies";
}
@Html.ActionLink("Back to Movies", "Edit")
<h2>Edit</h2>

@using (Html.BeginForm())
{
    <div>
        
        <div>
            @Html.EditorFor(x => x.SelectedHobbies)
        </div>
        @Html.HiddenFor(x => x.Id)
        <input type="submit" value="Go!" />
    </div>
}

@*<form asp-controller="Hobby" asp-action="Index" method="post" class="form-horizontal" role="form">
    <fieldset class="form-group">
        <h3>Checkbox List</h3>
        <legend class="col-sm-3 control-label"></legend>
            <div class="checkbox checkboxlist col-sm-9">
                @{ 
                    for (int i = 0; i < Model.Hobbies.Count(); i++)
                    {
                        <input asp-for="@Model[i].CheckboxAnswer" type="checkbox" />
                        <label asp-for="@Model.Hobbies[i].Id">@Model.Hobbies[i].Name</label>
                        <input asp-for="@Model.Hobbies[i].Id" type="hidden" />
                        <input asp-for="@Model.Hobbies[i].Name" type="hidden" />
                        <br />
                    }
                }

            </div>
        <div class="form-group">

            <div>
                @Html.Partial("CheckBoxListItem")
            </div>
            <div class="row">
                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-4">
                    <input id="Submit" type="submit" value="Submit" />  
             </div>
            </div>
        </div>
    </fieldset>
</form>*@
